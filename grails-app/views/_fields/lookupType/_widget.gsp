

<input type="text" id="${property}-display" name="${property}-display" value="${value?.name}" class="form-control ${clazz}" readonly="readonly"/>
<input type="hidden" id="${prefix}${property}.id" name="${property}" value="${value?.id}" class="form-control ${clazz}" readonly="readonly"/>